node_modules/
dist/
coverage/
*.tsbuildinfo

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
# Shared so the editor's built-in TS formatter cannot reflow files against the
# committed Prettier style on save.
!.vscode/settings.json

# Local secrets and personal overrides — never commit
.env
.env.*
*.pem
*.key
.npmrc.local
